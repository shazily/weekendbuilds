{
  "name": "weekendbuilds",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Weekend Labs — vibe-coded builds with kids",
  "scripts": {
    "deploy": "vercel --prod",
    "deploy:preview": "vercel",
    "test:submit": "node scripts/test-submit-flow.mjs",
    "test:submit:dispatch": "node scripts/test-submit-flow.mjs --dispatch"
  }
}
