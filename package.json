{
  "name": "weekendbuilds",
  "private": true,
  "version": "0.1.0",
  "description": "Weekly vibe-coded builds — starting with Vibe with Kids",
  "scripts": {
    "deploy": "vercel --prod",
    "deploy:preview": "vercel"
  }
}
